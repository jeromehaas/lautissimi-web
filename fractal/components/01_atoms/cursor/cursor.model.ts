export interface positionInterface {
	x: number;
	y: number;
};

export interface mainInterface {
	element: HTMLElement;
};

export interface followerInterface {
	element: HTMLElement;
	isStuck: boolean;
};